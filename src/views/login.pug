extends base.pug 

block content 
  h1 LOG IN
  if errorMessage 
        span=errorMessage 
  form(method="POST")
      input(placeholder="Your ID",name="userId",type="text",required)
      input(placeholder="Password",name="password",type="password",required)
      input(type="submit",value="Login")